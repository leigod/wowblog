# 开发环境依赖 = 运行时依赖 + 测试/代码质量工具
# 用法：pip install -r requirements-dev.txt（生产镜像只用 requirements.txt）
-r requirements.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
coverage[toml]>=7.5.0

# Code quality
ruff>=0.6.0
mypy>=1.11.0
black>=24.0.0

# Type stubs
types-requests>=2.31.0

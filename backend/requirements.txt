# Core dependencies
pydantic>=2.0.0
email-validator>=2.0.0   # pydantic EmailStr 必需依赖（注册接口 email 字段）
uvicorn[standard]>=0.20.0
gunicorn>=21.0.0   # 生产 ASGI 服务器(配合 -k uvicorn.workers.UvicornWorker 跑 FastAPI,宝塔 gunicorn 启动方式需要)
fastapi>=0.95.0
sqlalchemy>=2.0.0
pypinyin>=0.49.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

# Database
aiomysql>=0.2.0
pymysql>=1.1.0

# Cache & Rate Limiting
redis>=5.0.0

# Database Migrations
alembic>=1.13.0

# Authentication & Security
pyjwt>=2.8.0
# 密码哈希直接使用 bcrypt 库（app/utils/auth.py）；已弃用 passlib
# passlib 1.7.4 与 bcrypt 4.x 后端初始化不兼容，hash/verify 均抛异常
bcrypt>=4.0.0,<5.0.0

# WebSockets
websockets>=12.0

# HTTP clients
httpx>=0.27.0
socksio>=1.0.0          # httpx SOCKS 代理支持（OAuth 经系统代理访问 GitHub/Google 时需要）
requests>=2.31.0

# Image processing
pillow>=10.0.0

# OAuth providers
authlib>=1.3.0          # OAuth2/OIDC 客户端，接管 state/PKCE/nonce/id_token 验签
itsdangerous>=2.1.0     # SessionMiddleware 签名 cookie 依赖
pyyaml>=6.0.1
cryptography>=42.0.0

# 开发/测试/代码质量工具（pytest、ruff、mypy、black 等）不在本文件——
# 运行时不需要，混入会增大生产镜像、拖慢构建。开发环境请安装 requirements-dev.txt。
